{
  "name": "modalizer",
  "version": "0.0.2",
  "description": "Native fancy modals",
  "main": "index.js",
  "scripts": {
    "dev-styles": "sass -w assets/scss/index.scss dist/assets/styles/styles.css",
    "dev": "tsc -w",
    "build-styles": "sass --no-source-map --style=compressed assets/scss/index.scss dist/assets/styles/styles.css && sass --no-source-map --style=compressed assets/scss/animations/animate.scss dist/assets/styles/animate.css",
    "build-script": "rollup --compact -c rollup.config.js",
    "build": "rm -r dist && npm run build-styles && npm run build-script"
  },
  "author": "Chema Alfonso <hola@chemaalfonso.com>",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^11.1.5",
    "rollup": "^4.1.5",
    "sass": "^1.69.5",
    "typescript": "^5.2.2"
  },
  "type": "module",
  "dependencies": {
    "tslib": "^2.6.2"
  }
}
